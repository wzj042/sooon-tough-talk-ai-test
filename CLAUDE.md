
## Git 提交约定
- 使用 Conventional Commits：`<type>: <description>`，需要作用域时使用 `<type>(<scope>): <description>`。
- 提交正文按以下格式列出影响面：

```text
feat: 提交了一个功能

- 相关文件：改动的部分和原因
- 验证方式：执行过的检查或测试
```

- 常用 type：`feat`、`fix`、`docs`、`test`、`refactor`、`chore`。
- 初始结构提交使用：`chore: 初始化代理复用结构`。